datasources:
  - name: Loki
    label: Loki
    type: loki
    access: proxy
    url: loki:3100
    jsonData:
      maxLines: 10000

  - name: Prometheus
    label: Prometheus
    type: prometheus
    access: proxy
    url: prometheus:9090
    isDefault: true